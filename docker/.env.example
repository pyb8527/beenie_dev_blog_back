# docker-compose 의 backend 서비스가 env_file 로 읽는 실제 시크릿.
# 이 파일을 docker/.env 로 복사해 실제 값을 채운다. docker/.env 는 git 에 커밋되지 않는다.
JWT_SECRET=change-me-to-a-32+char-random-secret
GITHUB_CLIENT_SECRET=your-github-oauth-client-secret
